# Documentation

- File processing:
    - Runs locally (does not communicate with server)
    - Executes code on button presses
        - But cannot use only JS code (probably will need shell scripting)
    - Key functions to be implemented:
        -